const interval = (state = null, action) => {

    switch(action.type){

        case 'SET_INTERVAL':
            return action.payload

        default:
            state
    }
}

export default interval